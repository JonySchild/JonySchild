{{config(materialized='view')}}
SELECT C.C_CUSTKEY,C.C_NAME,C_NATIONKEY, SUM(O_TOTALPRICE) as TotalOrderPrice FROM SNOWFLAKE_SAMPLE_DATA.TPCH_SF1.CUSTOMER C
LEFT JOIN SNOWFLAKE_SAMPLE_DATA.TPCH_SF1.ORDERS O ON O.O_CUSTKEY = C.C_CUSTKEY
GROUP BY
    C.C_CUSTKEY
    ,C_NAME
    ,C_NATIONKEY